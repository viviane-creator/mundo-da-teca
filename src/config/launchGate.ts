/**
 * Gate temporário de lançamento.
 * Para voltar a Home em "/", altere `LAUNCH_MODE` para false.
 */
export const LAUNCH_MODE = true

/** Rota interna da Home durante o período de abertura */
export const HOME_PREVIEW_PATH = "/home-preview"
